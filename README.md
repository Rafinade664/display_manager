# display_manager
Windows display management utility. Allows display rotation (portrait/landscape) and setting unique backgrounds for each orientation on the selected display. Settings stored in the registry. Automatically applies the correct background upon orientation change. Multi-monitor support. Requires Windows 8+. Win32 API interface, written in C++.
